{
  "name": "alexa-plus-lambda",
  "version": "1.0.0",
  "description": "Alexa Plus Portal – Lambda handler for n8n/Echo Show integration",
  "main": "lambda_handler.js",
  "scripts": {
    "package": "zip -r lambda_function.zip lambda_handler.js node_modules",
    "test": "node -e \"require('./lambda_handler')\" && echo 'Syntax OK'"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
